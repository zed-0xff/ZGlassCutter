# see https://github.com/zed-0xff/zbspec

unpause: true
sandbox: false

log_packets: false

mods:
  - { id: "ZGlassCutter", path: "." }
  - ZBBetterFPS

# TODO: implement
# SandboxVars:
#   MinutesPerPage: 0.001

ready_condition: |
  local player = getPlayer() or getOnlinePlayers():get(0)
  return player and not player:tooDarkToRead()

game_versions:
  - 42.13.1
  - 42.13.2
  - unstable
